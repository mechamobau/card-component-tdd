describe("Card | component | integration test", () => {
  it.todo("should display the title when the title prop is passed");
  test.todo("if children element is showed when the children is passed");
  it.todo("should call 'onClick' when the show button is clicked");
});
